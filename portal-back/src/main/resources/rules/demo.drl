package customerTest;
import com.example.portalback.customer.model.CustomerBaseInfoModel
dialect "java"

rule "test01"
    no-loop true
    when
        $k:CustomerBaseInfoModel(true)
    then
        $k.setCreditScore("70");
end
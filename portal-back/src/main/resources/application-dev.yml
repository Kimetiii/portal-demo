spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/drools?useSSL=false&useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
      username: root
      password: root
      driver-class-name: com.mysql.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
localpath:
  template: D:\test\daily\template
